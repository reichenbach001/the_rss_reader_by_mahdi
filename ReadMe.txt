#To run the program, in the current folder type comman below:
#docker-compose up -d

#If container rss_reader.py failed in first time after running the docker-compose command, run the rss_reader container again!